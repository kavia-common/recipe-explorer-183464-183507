{
  "name": "recipe-frontend-ui",
  "version": "1.0.0",
  "private": true,
  "description": "User interface for the recipe application allowing users to browse and search for recipes.",
  "scripts": {
    "start": "vite",
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview --host 0.0.0.0 --port ${PORT:-3000}",
    "lint": "eslint src --ext .js,.jsx",
    "format": "prettier --write .",
    "test": "echo \"No test runner configured for Vite setup\" && exit 0"
  },
  "dependencies": {
    "axios": "^1.7.7",
    "react": "^18.3.1",
    "react-dom": "^18.3.1",
    "react-router-dom": "^6.26.2"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.3.3",
    "eslint": "^9.14.0",
    "prettier": "^3.3.3",
    "vite": "^5.4.10"
  }
}
